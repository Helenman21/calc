//export const screen = document.querySelector('.screen');

/*export function setEventListener(selector) {
	const element = document.querySelector(selector)

	element.addEventListener('click', (event) => {
		const value = event.target.textContent;
		pressButton(value);
	})
};*/



export const multiply = document.querySelector('.multiply');

export const minus = document.querySelector('.minus');

export const eguals = document.querySelector('.eguals');

export const clears = document.querySelector('.clears');

export const deletion = document.querySelector('.deletion');

export const division = document.querySelector('.division');

export const plus = document.querySelector('.plus')